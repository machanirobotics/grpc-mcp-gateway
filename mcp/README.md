# mcp/protobuf

> [!NOTE]
> This package contains **generated Go code** from the MCP annotation proto definitions in [`proto/mcp/protobuf/`](../proto/mcp/protobuf/).
> Do not edit these files manually — regenerate them with `cd proto && buf generate`.

These types are used by `protoc-gen-go` when compiling any `.proto` that imports `mcp/protobuf/annotations.proto`. They register the MCP extension fields on `google.protobuf.ServiceOptions` and `google.protobuf.MethodOptions`.
